{
  "system": {
    "database": {
      "host": "${DB_HOST}",
      "port": "${DB_PORT}",
      "user": "${DB_USER}",
      "password": "${DB_PASSWORD}",
      "database": "${DB_NAME}",
      "createDatabaseIfNotExist": true,
      "sslmode": "disable"
    },
    "theme": "eve",
    "extensions": []
  }
}